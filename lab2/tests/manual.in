# integers
1;

# floats
1.;
.1;
1.1;
1e10;
1e-10;

# strings
"aaa";
"!@#$^%@^^&";
"aaa bbb ccc";

# ids
A;
B;
_asd;
_1as;
as1;
aew_asd;

# comparators
A == A;
A != A;
A < A;
A > A;
A <= A;
A >= A;

# unary operators
-A;

# binary operators
A + A;
A - A;
A * A;
A / A;
A .+ A;
A .- A;
A .* A;
A ./ A;

# functions
eye(A);
zeros(A);
ones(A);

# matrices
[ A ];
[ A, A ];
[ A, A; A, A ];
[ A, A; A, A, A ];
[ A, A;
  A, A;
  A, A ];
[ [ A ], [ A, [ A ] ] ];

# assignments
A = A;
A += A;
A -= A;
A *= A;
A /= A;
A = A = A;

# code block
{ }
{ A; }
{ A; A; }
{ { } }
{ { A; } A; }

# while
while(A) A;
while(A) { A; }

# for
for A = A:A A;
for A = A:A { A; }

# print
print A;
print A, A;

# if
if(A) A;
if(A) { A; }
if(A) {
    A;
}

# if else
if(A) A; else A;
if(A) { A; } else { A; }

# if else if
if(A) A; else if(A) A;
if(A) A; else if(A) A; else if(A) A;
if(A) { A; } else if(A) { A; }
if(A) {
    A;
} else if(A) {
    A;
}
